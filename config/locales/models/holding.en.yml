en:
  activerecord:
    errors:
      models:
        holding:
          attributes:
            portfolio:
              taken: already has a holding with this coin
  holdings:
    create:
      success: '%{ticker} added to portfolio'
      fail: Can't add coin to portfolio
    update:
      success_deposit: 'Deposited %{amount} %{ticker} to portfolio'
      success_withdraw: 'Withdrew %{amount} %{ticker} from portfolio'
      success_update: 'Updated %{ticker} to %{amount}'
    destroy:
      success: 'Removed %{ticker} from portfolio'