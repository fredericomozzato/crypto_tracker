en:
  activerecord:
    errors:
      models:
        holding:
          attributes:
            portfolio:
              taken: already has a holding with this coin
  holdings:
    create:
      success: '%{ticker} added to portfolio'
      fail: Can't add coin to portfolio
    update:
      success: 'Deposited %{amount} %{ticker} to portfolio'